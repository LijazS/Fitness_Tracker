apiVersion: v1
kind: persistentVolumeReclaimClaim
metadata: 
  name: fitness-pvc
  namespace: l-ns
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: "8Gi"
  # selector:
  #   matchLabels:
  #     app: "fitness" 
