apiVersion: policies.kyverno.io/v1alpha1
kind: ValidatingPolicy
metadata:
  name: require-labels
spec:
  validationActions:
    - Deny
  matchConstraints:
    resourceRules:
      - apiGroups: ['']
        apiVersions: ['v1']
        operations: ['CREATE', 'UPDATE']
        resources: ['Namespace']
  validations:
    - message: "label 'hr' is required"
      expression: "has(object.metadata.labels) && has(object.metadata.labels.hr) && object.metadata.labels.team != ''"
